# 微信小游戏接入与发布指南

> 适用于 Harbor Loop 当前的原生 JavaScript + Canvas 2D 工程。  
> 最后核对日期：2026-08-03。微信后台菜单名称和审核要求可能调整，正式发布时以微信公众平台当前页面提示为准。

## 先确认：这是“微信小游戏”，不是普通“微信小程序”

导入项目时必须选择 **小游戏（Mini Game）**。如果误选“小程序”，常见结果是提示缺少 `app.json`、页面文件或 WXML。

本工程已经包含小游戏入口和配置：

```text
racer/
├── game.js
├── game.json
├── project.config.json
├── index.html                 # 仅用于浏览器快速测试
├── README.md
├── NOTES.md
└── WECHAT_ONBOARDING.md
```

微信环境主要使用 `game.js`、`game.json` 和 `project.config.json`；`index.html` 只用于 Chrome 浏览器预览。

---

## A. 只在电脑上快速测试

### 方式 1：Chrome

1. 解压项目。
2. 双击 `index.html`。
3. 使用 Chrome 打开。
4. 用 `← / →` 或 `A / D` 换道。

这个方式最快，但不能完整模拟微信手机环境、触控、震动、分享或排行榜。

### 方式 2：微信开发者工具模拟器

1. 下载并安装官方 **微信开发者工具**：  
   https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. 使用微信扫码登录。
3. 在首页选择 **小游戏**。
4. 点击 **导入项目**。
5. 项目目录选择包含 `game.js` 的根目录，不要选 ZIP，也不要多套一层文件夹。
6. 本地早期测试可继续使用 `project.config.json` 中的：

```json
"appid": "touristappid"
```

7. 点击 **编译**，游戏会出现在模拟器中。

`touristappid` 适合本地模拟器验证，但正式手机体验、体验成员、上传和发布需要你自己的小游戏 AppID。

---

## B. 注册正式微信小游戏并取得 AppID

1. 打开微信公众平台：  
   https://mp.weixin.qq.com/
2. 注册一个新的小程序/小游戏账号，并按后台流程完成邮箱、主体和管理员信息。
3. 创建时选择游戏相关类目。具体主体限制、资质、备案或审核材料，以后台实时要求为准。
4. 登录小游戏管理后台。
5. 在 **开发管理 / 开发设置** 一类的页面找到 **AppID**。

### 不要做的事

- 不要把 `AppSecret` 写入 `game.js`、GitHub 或任何前端文件。
- 不要把个人 token、私钥或公司代码提交到公开仓库。
- AppID 可以出现在客户端配置中；AppSecret 必须只保存在可信后端或微信云环境中。

---

## C. 把正式 AppID 放进本项目

打开 `project.config.json`，将：

```json
"appid": "touristappid"
```

替换为：

```json
"appid": "你的正式小游戏 AppID"
```

例如：

```json
{
  "compileType": "game",
  "appid": "wx1234567890abcdef",
  "projectname": "harbor-loop-prototype"
}
```

保存后，在微信开发者工具中重新导入项目，或关闭再打开工程。

建议不要频繁提交真实 AppID 的变化；如果以后需要多个环境，可以另建本地配置方案。

---

## D. 在自己手机微信里预览

1. 在微信开发者工具中打开项目。
2. 确认模拟器里运行正常。
3. 点击工具栏的 **预览**。
4. 工具会上传一个开发预览版本并显示二维码。
5. 用绑定该小游戏的管理员或开发者微信扫码。
6. 游戏会直接在手机微信里打开。

重点检查：

- 左右半屏触控是否准确。
- 不同手机比例下赛道是否被裁切。
- 刘海、灵动岛、底部手势区域是否遮挡 Combo。
- 暂停到后台再回来是否正常。
- 真机帧率、发热和音效延迟。

如果其他人也要测试，需要在微信后台将其加入开发成员或体验成员；具体菜单可能显示为 **成员管理**、**项目成员** 或 **体验成员**。

---

## E. 上传一个体验版本

当本地和手机预览都正常后：

1. 在微信开发者工具右上角点击 **上传**。
2. 填版本号，例如：

```text
0.8.1
```

3. 填版本说明，例如：

```text
Minimal HUD, long track, AI lane-change safety zone.
```

4. 上传完成后登录微信公众平台。
5. 进入 **版本管理**。
6. 在开发版本中找到刚上传的版本。
7. 按后台选项将它设为体验版，或生成体验二维码。
8. 将测试者加入体验成员后，让他们扫码测试。

开发者工具的“预览”和后台的“体验版”用途相近，但体验版更适合多人、较长期测试和提审前确认。

---

## F. 提交审核与发布

1. 在微信公众平台进入 **版本管理**。
2. 选择准备上线的开发版本。
3. 点击 **提交审核**。
4. 按页面填写当前要求的信息，常见项目包括：
   - 游戏名称和简介
   - 类目与玩法说明
   - 游戏截图或录屏
   - 测试账号或操作路径（如适用）
   - 隐私相关说明
   - 版本更新内容
   - 后台要求的主体或游戏资质材料
5. 审核期间不要假设材料要求固定；以后台当次显示为准。
6. 审核通过后，回到版本管理点击 **发布**。

审核通过通常不会自动上线，仍需你在后台确认发布。

---

## G. 以后每次更新的标准流程

### 1. 将新版文件覆盖到本地 Git 仓库

例如你的仓库目录是：

```text
~/Documents/racer
```

把新版 ZIP 解压后的文件复制进去并替换同名文件。不要把整个版本文件夹再套一层。

### 2. 提交 GitHub

```bash
cd ~/Documents/racer
git status
git add .
git commit -m "Update Harbor Loop to v0.8.1"
git push
```

### 3. 微信开发者工具重新编译

如果工具没有自动刷新：

- 点击 **编译**；
- 或关闭项目再重新打开；
- 遇到缓存问题时，在开发者工具中清缓存后重新编译。

### 4. 手机回归测试

至少测试：

- 方向键和左右半屏触控
- AI 变道安全距离
- 高速 Combo
- 撞车恢复
- 前后台切换
- 不同屏幕尺寸

### 5. 上传新版本

在开发者工具点击 **上传**，使用新的版本号和版本说明。然后在后台设为体验版、提交审核或发布。

---

## H. 当前项目是否需要服务器或域名

当前 Harbor Loop 是纯本地 Canvas 游戏：

- 没有登录
- 没有排行榜
- 没有云存档
- 没有广告
- 没有远程图片或音频

因此当前原型通常不需要配置业务服务器域名。

以后增加以下功能时，才需要额外接入：

- 好友或全球排行榜
- 用户登录资料
- 云存档
- 远程资源下载
- 支付、广告、数据分析

届时要在微信后台配置合法域名或使用微信云开发，并确保所有网络连接使用当前平台要求的 HTTPS/TLS 配置。

---

## I. 常见问题

### 1. 导入后提示缺少 `app.json`

原因通常是工程类型选成了“小程序”。返回首页，重新选择 **小游戏** 再导入。

### 2. 导入后空白或找不到 `game.js`

选择错了目录。应选择直接包含 `game.js` 的项目根目录。

### 3. Chrome 能运行，微信开发者工具不能运行

浏览器和微信小游戏不是完全相同的运行环境。查看开发者工具 Console，优先排查：

- 使用了浏览器专属 API
- Canvas 初始化方式不兼容
- 文件路径或大小写错误
- 缓存了旧版本

### 4. 手机扫码后别人打不开

确认：

- 使用的是正式小游戏 AppID，不是 `touristappid`
- 对方已加入体验成员或项目成员
- 上传/体验版本仍然有效

### 5. 真机和模拟器比例不同

不要把 UI 写死在固定像素位置。所有 HUD、触控区和赛道缩放都应基于实际屏幕宽高和安全区域计算。

### 6. 更新后仍显示旧代码

依次尝试：

1. 重新点击编译。
2. 清理微信开发者工具缓存。
3. 删除后重新导入项目。
4. 手机端退出小游戏后重新扫码。
5. 上传时确认版本号和说明已变化。

---

## 官方入口

- 微信开发者工具下载：  
  https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
- 微信公众平台：  
  https://mp.weixin.qq.com/
- 微信小游戏开发文档入口：  
  https://developers.weixin.qq.com/minigame/dev/guide/

